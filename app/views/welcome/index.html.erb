<div class="container set-max-width">
    <div class="jumbotron">
        <div class="container">
            <% if current_user %>
                <h1>Welcome to BlocMetrics!</h1>
                <p>Your new favorite analytics app!</p>
                <%= link_to "Dashboard", registered_applications_path, class:"btn btn-success btn-lg" %>
            <% else %>
                <h1>Welcome to BlocMetrics!</h1>
                <p>Your new favorite analytics app!</p>
                <%= link_to "Sign Up!", new_user_registration_path, class:"btn btn-primary btn-lg" %>
                <%= link_to "Sign In!", new_user_session_path, class:"btn btn-success btn-lg" %>
            <% end %>
        </div>
    </div>
</div>